/**
 * Firebase Client SDK — re-exports from the canonical config location.
 *
 * This file exists at `src/firebase/firebase.js` as specified in the
 * project requirements. The actual configuration lives in
 * `src/config/firebase.js` to match the existing project structure.
 */
export { auth, googleProvider } from '../config/firebase';
